# Mini Project

## Setup

1. Install dependencies:
   `npm install`
2. Check that Svelte is installed:
   `npm ls svelte`
3. Start the dev server:
   `npm run dev`

This workspace is set up as a SvelteKit app and uses `src/lib` for shared components.
